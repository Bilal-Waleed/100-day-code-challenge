// No Users: Ensure your user list isn’t empty.

let usernames: string[] = []; // Empty array

if (usernames.length === 0) {
    console.log("We need to find some users!");
} else {
    
}
// removing all username ensure the message 